{
  "name": "@diary-spo/mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rsbuild build && bun run build:runner",
    "build:runner": "bun build src/background/grade-runner.ts --outfile dist/runners/grade-runner.js --target browser --format iife",
    "build:android": "bun run sync && cd android && ./gradlew assembleDebug",
    "sync": "bun run build && cap sync android",
    "open:android": "cap open android",
    "run:android": "bun run sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/background-runner": "3.0.0",
    "@capacitor/core": "8.5.1",
    "@diary-spo/crypto": "workspace:*",
    "@diary-spo/diary-client": "workspace:*",
    "@diary-spo/shared": "workspace:*",
    "@diary-spo/web": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1",
    "@rsbuild/core": "^1.2.3",
    "@rsbuild/plugin-react": "^1.1.0",
    "typescript": "^5.7.3"
  }
}
