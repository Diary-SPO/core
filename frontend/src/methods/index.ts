import { getCookie } from './getCookie';
import { getMarks } from './getMarks';
import { getUserId } from './getUserId';
import { getLessons } from './getLessons';

export {
  getCookie, getMarks, getUserId, getLessons,
};
